machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 4.2.0

dependencies:
  cache_directories:
    - ~/.cache/yarn
    - ${HOME}/${CIRCLE_PROJECT_REPONAME}/test/fixture/elm-stuff"
  override:
    - if [ ! -d sysconfcpus/bin ];
      then
        git clone https://github.com/obmarg/libsysconfcpus.git;
        cd libsysconfcpus;
        ./configure --prefix=$TRAVIS_BUILD_DIR/sysconfcpus;
        make && make install;
        cd ..;
      fi
    - yarn

test:
  override:
    - yarn test
